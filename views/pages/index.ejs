<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>
    <form action="/SearchBook" method="POST">
    <input type="text" name="book" required placeholder="Title/Author" required>
    <label></label><input value="intitle" name="bookSearch" type="radio" required>Title</label>
    <label><input type="radio" value="inauthor" name="bookSearch" required>Author</label>
    <button>Go</button>
    </form>
    <% savedBooks.forEach(book =>{ %> 
    <div>
    <img src="<%= book.image_url %> " alt="book">
    <p> <%= book.title %>  </p>
    <p> <%= book.author %>  </p>
    <p> <%= book.isbn %>  </p>
    <p> <%= book.date %>  </p>
    <p> <%= book.description %>  </p>
    <form action="/details/<%= book.id%> " method="GET">
    <button>See Book Details</button>
    </div>
    </form>
    <% }) %> 
</body>
</html>